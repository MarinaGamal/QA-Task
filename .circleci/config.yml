version: 2.1

orbs:
  node: circleci/node@4.7
  browser-tools: circleci/browser-tools@1.4.1

jobs:
  ContactUs:
    docker:
      -  image: cimg/node:lts-browsers
    working_directory: ~/QA-Task/UI/NightWatch
    steps:
      - checkout:
          path: ~/QA-Task

      - browser-tools/install-chrome
      - node/install-packages   

      - run:
          name: Run Contact Us
          command: 
            npx nightwatch test/MyStore/ContactUs.js

  SearchDress:
    docker:
      -  image: cimg/node:lts-browsers
    working_directory: ~/QA-Task/UI/NightWatch
    steps:
      - checkout:
          path: ~/QA-Task      
      - browser-tools/install-chrome
      - node/install-packages 

      - run:
          name: Run Search Dress
          command: 
            npx nightwatch test/MyStore/SearchDress.js

  APITest:
    docker:
      -  image: cimg/node:lts-browsers
    working_directory: ~/QA-Task/API/MockUser
    steps:
      - checkout:
          path: ~/QA-Task  
      - run:
          name: "Install Mock-User-Auth and Supertest"
          command: |
            npm install mock-user-auth
            npm install supertest --save-dev
            npm install --save-dev mocha
            npm install --save-dev mochawesome
      - run:
          name: "Start Mock-User-Auth server"
          background: true
          command: "npm run dev"
          working_directory: API/MockUser

          
      - run:
          name: "Execute API tests"
          working_directory: API/MockUser
          command: "npx mocha api.test.js"
         
    

workflows:
  test:
    jobs:
      - ContactUs
      - SearchDress
      - APITest